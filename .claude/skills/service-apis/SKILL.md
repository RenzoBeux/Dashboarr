---
name: service-apis
description: Upstream API reference and hard-won gotchas for every service Dashboarr integrates with (qBittorrent, rtorrent, Transmission, Deluge, NZBGet, SABnzbd, Radarr, Sonarr, Bindery, Prowlarr, Seerr/Overseerr, Jackett, NZBHydra2, Tautulli, Plex, Bazarr, Glances, Jellyfin, JellyStat, Autobrr, Cleanuparr, Navidrome, Pi-hole). Use when implementing, debugging, or extending any service integration in services/ or hooks/, or when verifying an endpoint shape, auth scheme, or field type.
---

# Service API Documentation (sources of truth)

When implementing or debugging a service integration, consult the upstream API docs below — these are the authoritative references. Prefer fetching the relevant doc page over guessing endpoint shapes.

| Service | API doc URL | Notes |
| --- | --- | --- |
| qBittorrent | https://github.com/qbittorrent/qBittorrent/wiki/WebUI-API-(qBittorrent-5.0) | WebUI API v2; cookie-session auth via `/api/v2/auth/login`. We target qBittorrent 5.0+. The 4.1 wiki page exists for older builds but is not our target. |
| rtorrent / ruTorrent | https://github.com/rakshasa/rtorrent/wiki/RPC-Setup-XMLRPC + https://docs.rtorrent.org/ | XML-RPC over the SCGI HTTP mount (conventionally `/RPC2`, works for bare rtorrent and ruTorrent); HTTP Basic auth. We use `d.multicall2` to list, `system.multicall` to batch actions/global stats, `load.start` (empty first target arg!) to add. Request XML is built by string concat; responses parsed with `fast-xml-parser` in `lib/xmlrpc.ts`. Status derived from `d.state`/`d.is_active`/`d.complete`/`d.hashing`/`d.message`; `d.ratio` is per-mille. Delete-with-data needs ruTorrent's erasedata plugin. |
| NZBGet | https://nzbget.com/documentation/api/ | JSON-RPC 2.0 over `POST /jsonrpc`; positional params only. HTTP Basic Auth using ControlUsername/ControlPassword from `nzbget.conf`. 64-bit byte counts split into Lo/Hi pairs — recombine via `combineHiLo()` in `lib/utils.ts`. |
| Radarr | https://radarr.video/docs/api/ | OpenAPI/Swagger; live spec also served by each instance at `/api/v3/openapi.json`. We use the `v3` API. |
| Sonarr | https://sonarr.tv/docs/api/ | OpenAPI/Swagger; live spec also at `/api/v3/openapi.json`. We use the `v3` API. |
| Bindery | https://github.com/vavallee/bindery — `docs/API.md`, with `cmd/bindery/main.go` as the authoritative route list | The Go successor to the archived Readarr (books + audiobooks). **Not an *arr fork**: no `/api/v3`, no Readarr shim. `/api/v1` is its own native API whose route names merely echo *arr. Its `docs/API.md` self-describes as "a representative selection" and is wrong in at least one place (it documents `/search/author?q=`; the handler reads only `?term=`), so read `cmd/bindery/main.go` and the handlers under `internal/api/` when verifying anything. No OpenAPI spec exists. See the Bindery notes below. |
| Prowlarr | https://prowlarr.com/docs/api/ | OpenAPI/Swagger; live spec also at `/api/v1/openapi.json`. We use the `v1` API. |
| Seerr (Overseerr) | https://api-docs.overseerr.dev/ | Same API for Jellyseerr forks. Schema validated by `express-openapi-validator` — unknown query params return 500 (see comment in `services/overseerr-api.ts`). |
| Jackett | https://github.com/Jackett/Jackett (no official API docs; controllers in `src/Jackett.Server/Controllers/`) | Auth is an `apikey` QUERY PARAM (not a header) and only the results/Torznab routes validate it — the admin REST API (`/api/v2.0/indexers`, `/server/config`, per-indexer config/test) requires the admin-password cookie and is off limits. Search via JSON `GET /api/v2.0/indexers/all/results?apikey&Query=` (fields are PascalCase); indexer list + ping via Torznab `GET /api/v2.0/indexers/all/results/torznab/api?t=indexers&configured=true` (XML, parsed with `fast-xml-parser` in `services/jackett-api.ts`). No server-side grab — the app hands MagnetUri/Link to a torrent client via the unified torrent adapter. |
| NZBHydra2 | GitHub wiki "External API, RSS and cached queries" (partly STALE — see below) + the Java sources in `core/src/main/java/org/nzbhydra/` of theotherp/nzbhydra2 | Usenet meta-search. One install-wide API key as an `apikey` QUERY PARAM (injected in `lib/http-client.ts` alongside Jackett's) **plus a copy in the JSON body** of the four stats/history endpoints. **No OpenAPI spec** (springdoc is commented out in `core/pom.xml`) and `/internalapi/**` needs a session cookie, not the key — so nothing may be built on it, which is why the indexer list is read-only. `GET /api?t=caps&o=json` is the connection probe: it validates the key, carries the version at `server["@attributes"].appversion`, and is the only call NOT behind `auth.allowApiStats`. `POST /api/stats/indexers`, `/api/stats`, `/api/history/searches`, `/api/history/downloads` are the rest. See the NZBHydra2 notes below. |
| Tautulli | https://github.com/Tautulli/Tautulli/wiki/Tautulli-API-Reference | Single endpoint: `/api/v2?apikey=…&cmd=…`. Not REST-shaped — see `tautulliRequest` in `services/tautulli-api.ts`. |
| Plex | https://plexapi.dev/ (community) + https://www.plexopedia.com/plex-media-server/api/ | Plex has no official public API docs; the community references above are the de facto sources. Auth via `X-Plex-Token`. |
| Bazarr | https://wiki.bazarr.media/ + live Swagger at `<bazarr>/api/swagger` | Each running instance exposes its own Swagger UI; the wiki covers setup, the Swagger UI is the authoritative endpoint reference. |
| Glances | https://glances.readthedocs.io/en/latest/api.html | REST API exposed when Glances runs in webserver mode (`-w`). We use API v4. |
| Jellyfin | https://api.jellyfin.org/ | OpenAPI; live spec also at `/api-docs/openapi.json`. Auth via `Authorization: MediaBrowser Token="…"`, **not** `X-Emby-Token`, which is one of the legacy shapes Jellyfin now gates behind a server flag. See the Jellyfin note below. |
| Navidrome | https://www.navidrome.org/docs/developers/subsonic-api/ + https://opensubsonic.netlify.app/docs/, with `server/subsonic/`, `server/nativeapi/`, `server/auth.go` and `model/library.go` of navidrome/navidrome as authoritative | Music server. **No API keys** — `validateCredentials` accepts only `p` / `t`+`s` / `jwt`, and it does NOT implement the OpenSubsonic `apiKey` extension. Auth is username + password; we send the salted token `t = md5(password + salt)`. Three roots on one host (`/rest` Subsonic v1.16.1, `/api` native react-admin with `X-ND-Authorization: Bearer`, `/auth/login`), so `apiBasePath` is empty and paths self-prefix. **Every Subsonic error is HTTP 200** — read the body. `GET /api/library` (admin) is the only source of total size / missing count; `DELETE /api/missing` with no `id` purges all. See the Navidrome notes below and `services/navidrome-api.ts`. |
| JellyStat | https://github.com/CyferShepard/Jellystat (live Swagger at `<host>/swagger`) | Jellyfin stats server (Tautulli-analog). Root-mounted REST (`/stats`, `/api`, `/proxy`); auth via `x-api-token` header. Postgres `bigint` columns (Count/Plays/PlaybackDuration) serialize as strings — coerce. Live sessions via `/proxy/getSessions` pass the raw Jellyfin payload through. See `services/jellystat-api.ts`. |
| Autobrr | https://autobrr.com/api + route handlers in `internal/http/` and TS shapes in `web/src/types/*.d.ts` of autobrr/autobrr | Auth via `X-API-Token` header (`?apikey=` also accepted; we send the header). JSON is snake_case. `GET /api/healthz/liveness` is **anonymous** — it answers 200 with a wrong key, so the connection probe validates against `GET /api/release/stats` instead. Release search: `GET /api/release?limit&offset&q&push_status` (`PUSH_APPROVED`/`PUSH_REJECTED`/`PUSH_ERROR`; an invalid value 400s, so omit when unfiltered). **`GET /api/irc/network/{id}/restart` is a GET that mutates** — keep it in a mutation, never a query. Filter toggle: `PUT /api/filters/{id}/enabled` body `{enabled}`. See `services/autobrr-api.ts`. |
| Deluge | https://deluge.readthedocs.io/en/latest/devguide/how-to/curl-jsonrpc.html + `deluge/ui/web/json_api.py`, `deluge/ui/web/auth.py`, `deluge/core/core.py`, `deluge/core/torrent.py` of deluge-torrent/deluge | Web UI JSON-RPC v1: `POST <base>/json`, body `{method, params, id}` with **all three keys required** and params **positional only**. We target Deluge 2.x. **Everything answers HTTP 200** — failures live in `{error: {message, code}}` (1 = not authenticated, 2 = unknown method, 3 = local exception, 4 = daemon exception, 5 = malformed request). Auth is a single Web UI **password, no username**, exchanged via `auth.login` for a `_session_id` cookie (sliding 1h expiry); replay the raw cookie value rather than trusting a path-aware jar, because the cookie's `Path` changed in 2.2.0. **`Content-Type` must be exactly `application/json`** — Deluge <= 2.0.5 string-compares it and rejects the `; charset=utf-8` most HTTP stacks append. **THE trap: deluge-web is a proxy to a separate deluged daemon, and until it has connected every `core.*`/`label.*` call answers `Unknown method` (code 2)** — so a fresh session must run `web.connected` -> `web.get_hosts` -> `web.connect(hostId)`, and any later code 2 (or a code 3 `AttributeError: 'NoneType'`) means the daemon went away and the handshake must be redone. `web.connect` fails SILENTLY (returns null with `error: null`), so verify its result is a non-empty method array. **Never send an empty `keys` list** to `core.get_torrents_status` — Deluge reads that as "every field" and returns files/peers/pieces for every torrent. Numeric traps: `progress` is **0-100** (but is pinned to 100 whenever `state` is `Error`, so recompute from `total_done`/`total_wanted` there), `file_progress` is **0-1**, `ratio` is `-1` for "nothing downloaded", `eta` is seconds with 0 = unknown and -1 = over a year, and `max_download_speed`/`max_upload_speed` are **KiB/s where any NEGATIVE value is unlimited and 0 is a real throttle**. `state` is one of Allocating/Checking/Downloading/Seeding/Paused/Error/Queued/Moving — there is **no Stalled** (that's Downloading at a zero rate) and **no turtle/alt-speed mode anywhere**. `pause_torrents`/`resume_torrents` take a list, but calling them with NO argument pauses the whole session; `remove_torrents` returns an **empty list on success** and `[[id, error]]` pairs on partial failure. Code-4 messages are a stringified Twisted `Failure` wrapping the real class and traceback — unwrap it before showing a user. Labels are an optional plugin (`label.*`), single-valued, and `label.add` lowercases while `label.set_torrent` does not. See `services/deluge-api.ts`. |
| Cleanuparr | https://cleanuparr.github.io/Cleanuparr/docs/configuration/stats (v2 stats) + C# controllers in `code/backend/Cleanuparr.Api/` of Cleanuparr/Cleanuparr | Auth via `X-Api-Key` header (or `?apikey=`); the key authenticates as a full user on EVERY `[Authorize]` controller (jobs, events, stats), not just stats. Anonymous `GET /health` is **root-mounted** (not under `/api`), so `apiBasePath` is empty and module paths self-prefix `/api` (JellyStat pattern). **v1 `/api/stats` is sunset 2026-09-01 — use `/api/v2/stats?hours=` only.** JSON is camelCase properties but **PascalCase enum strings** (`JsonStringEnumConverter`, no naming policy), and zero-activity breakdown keys are omitted, never sent as 0. `GET /api/events?page&pageSize&severity&eventType&search` → `{items,page,pageSize,totalCount,totalPages}`. `POST /api/jobs/{jobType}/trigger` — **`Seeker` refuses manual triggers (400)**. See `services/cleanuparr-api.ts`. |
| Pi-hole | https://docs.pi-hole.net/api/ (self-hosted OpenAPI at `<host>/api/docs`) + `src/api/`, `src/config/config.c` and `src/datastructure.c` of pi-hole/FTL | **v6 ONLY** — v6 replaced v5's PHP `/admin/api.php` with a REST API under `/api` served by FTL, with no compatibility shim, so a v5 host 404s every path (the probe detects it via `/admin/api.php?status` and says "needs v6" rather than "wrong password"). Auth is a session: `POST /api/auth` `{password}` -> `{session:{valid,sid,validity}}`, then **`X-FTL-SID`** on every later call; `DELETE /api/auth` logs out. **`GET /api/info/login` is anonymous** (`src/api/api.c` registers it auth-not-required) — a third entry in the Autobrr/NZBHydra2 false-green liveness trap, fine as `pingPath`, useless as a credential probe. **THE constraint: `webserver.api.max_sessions` defaults to 16** (`src/config/config.c`) with a 30-minute *idle* TTL, and the health poll runs every 30s per instance — so a login-per-probe exhausts the pool in ~8 minutes and locks the user out of their own admin UI. The SID is cached in `lib/pihole-session.ts`, shared with `runConnectionProbe` (which re-validates via `GET /api/auth` before ever minting one, and DELETEs any session it creates for an unsaved-form Test), and concurrent logins are de-duplicated. **`api_seats_exceeded` arrives as a 401**, identical at the status level to a wrong password — only `error.key` separates them. **FTL's error envelope is nested** (`{"error":{"key","message","hint"}}`), which the shared `getHttpErrorMessage` cannot see. A password-less Pi-hole answers `200 {valid:true, sid:null}` — cache `""`, not falsy, or you re-login forever. `GET/POST /api/dns/blocking`: `blocking` is a **four-value** enum (enabled/disabled/failed/unknown — never render it as a boolean) and `timer` is **remaining seconds at answer time**, not a deadline, so anchor countdowns to React Query's `dataUpdatedAt`; POST echoes the new state. **`POST /api/action/gravity` streams chunked `text/plain`** (RN's fetch buffers it — no incremental progress is possible) for 2-5 minutes, is **not** gated by `allow_destructive` (only restartdns and `flush/*` are), and cannot be cancelled — treat a timeout as "still running" and confirm via `stats/summary`'s `gravity.last_update`. A `[✗]` line means one unreachable blocklist, not a failed run. `GET /api/history`: **`total` is the SUM of cached+blocked+forwarded**, not a fourth series. `GET /api/config/dns/cnameRecords` returns the **nested subtree** `{config:{dns:{cnameRecords}}}`, and `PUT|DELETE /api/config/dns/cnameRecords/{value}` matches the stored string **byte-for-byte** — delete with the raw value, never a re-formatted one, and URL-encode it yourself (`buildUrl` does not encode paths). `CACHE_STALE` is **not** a blocked status. Verified against pi-hole/FTL@master. See `services/pihole-api.ts`, `lib/pihole-normalize.ts` and `lib/pihole-session.ts`. |

Notes:
- **Bindery diverges from the *arr family in ways that bite.** All verified against `vavallee/bindery@main`:
  - **Auth.** One install-wide API key. `X-Api-Key` always works; `?apikey=` is honoured on **GET/HEAD/OPTIONS only**. API-key requests bypass both the `X-Requested-With` and CSRF guards, so mutations need no extra headers. `/api/v1/health` is on the server's unauthenticated allowlist and answers 200 without a key — probe `/api/v1/system/status` instead, or a wrong key reads as healthy.
  - **Three envelope shapes in one API.** `{items,total,limit,offset}` on `/author`, `/book`, `/history` (paginate by **offset**, never `page`; `limit` above 500 is silently clamped and echoed back, so advance by what you received); `{items,partial?,staleClients?}` on `/queue`; a bare array on `/wanted/missing`, `/rootfolder`, `/qualityprofile`, `/metadataprofile`, `/blocklist`, `/pending`, `/indexer`. `/history` already changed from bare array to envelope once. `lib/bindery-normalize.ts` absorbs all of them.
  - **`GET /book?authorId=N` silently drops every other filter.** That branch honours only `authorId`, `includeExcluded` and in-memory limit/offset — `status`, `search`, `sort`, `mediaType`, `monitored` and the release bounds are ignored, and it orders by release date rather than title. It still answers 200, so the bug is invisible. Use the embedded `books[]` on `GET /author/{id}` instead.
  - **`availableBookCount` and `wantedBookCount` are always 0.** `AuthorStats` is built in exactly one place (`internal/db/authors.go`) which sets `bookCount` alone. `statistics` is also present only on the LIST response; `GET /author/{id}` omits the key. Any progress bar derived from those two fields reads 0% or 100% for every author forever.
  - **`editions[]` is dead on the wire.** `models.Book` declares it, but no handler populates it and there is no `/book/{id}/editions` route. `bookFiles[]` and `identifiers[]` (single-book reads only) are the real data.
  - **Partial updates are safe.** `PUT /author/{id}` and `PUT /book/{id}` decode every field as a pointer and apply only what is present — send just the changed field. Two consequences: JSON `null` is indistinguishable from omission, so a profile **cannot be cleared** (only `audiobookRootFolderId`, via `clearAudiobookRootFolder`); and sending `title`/`description`/`genres`/`language`/`releaseDate` also **locks that field** against metadata refresh, so never include them in a monitor toggle.
  - **Images.** `imageUrl` is a relative proxy path, `<BINDERY_URL_BASE>/api/v1/images?url=<encoded remote>`, on both list and detail responses. Rebuild it from the embedded `url=` param rather than passing it through, or a subpath deploy double-prefixes. Queue rows, `/wanted/missing` and `/search/*` stubs are **not** proxied.
  - **Absent entirely:** `/command`, `/calendar`, `/diskspace`, `/tag`, `/queue/details`, `/release`, `/wanted/cutoff`, an *arr-style `/health` issue array, and SignalR/websockets. `/queue` takes no query parameters at all. There is no blocklist option on any queue-removal route.
- **Servarr `POST <provider>/testall` answers 400 on the happy path.** `ProviderControllerBase.TestAll()` (shared by Radarr/Sonarr/Lidarr/Prowlarr's indexer, downloadclient, applications, importlist and notification controllers) ends with `return result.Any(c => !c.IsValid) ? BadRequest(result) : Ok(result);` — so whenever any provider fails its test you get `400` **carrying the full result array**, not an error envelope. Treat that body as the outcome (`[{ id, isValid, validationFailures: [{ propertyName, errorMessage }] }]`) instead of an HTTP failure, and resolve `id` → name with a GET on the same route minus `/testall`. See `services/arr-health.ts`.
- **NZBHydra2's wire format has several traps, and its wiki is stale.** All verified against `theotherp/nzbhydra2@master`:
  - **The two halves of the API fail in completely different ways, and the split is not obvious.** `/api` (the newznab mount, `ExternalApi`) answers **HTTP 200 for every error**: its `@ExceptionHandler` returns a `NewznabXmlError` with the default 200 status, so a wrong key comes back as `{"code":"100","description":"Wrong api key"}`, never a 401 — the probe and the search path must read the BODY. But `/api/stats*` and `/api/history/*` live in `ExternalApiStats`, a **separate `@RestController` that inherits none of that** (`@ExceptionHandler` is controller-local, and the project's one `@ControllerAdvice` lists only Spring's own exception types), so their `IllegalAccessException` reaches no handler and Tomcat renders a stock **`500 text/html`** page. Verified against 8.9.0: wrong key on `/api?t=caps` → 200 JSON error envelope; wrong key OR `allowApiStats: false` on `/api/stats/indexers` → 500 HTML. Because the body is HTML, `serviceRequest` surfaces it as an `AuthProxyResponseError`, so anything classifying that failure has to key on `status`, not the message. `readHydraJsonError` / `readHydraXmlError` handle the 200-envelope half; `isStatsApiGated` handles the 500 half.
  - **Newznab JSON attribute holders come back under EITHER `"@attributes"` or `attributes`.** Upstream declares `@JsonProperty("@attributes")` on `NewznabJsonItemAttr`, `NewznabJsonEnclosure`, `NewznabJsonChannelResponse` and `CapsJsonServer` — but the **GraalVM native build loses the rename** and emits the bare field name. Both mainstream Docker images (hotio and linuxserver) ship that native binary, so `attributes` is what a containerised instance actually sends; verified on 8.9.0, where `t=caps` answers `server.attributes.appversion` and a search answers `channel.response.attributes`. Reading only one spelling type-checks against a hand-written interface and fails **silently**: every row's indexer name falls back to "NZBHydra2" and every size reads 0. Go through `hydraHolder()` in `lib/nzbhydra2-normalize.ts`, which accepts either.
  - **Since v7.15.3 (2025-07-04) `/api/stats`, `/api/stats/indexers`, `/api/history/searches` and `/api/history/downloads` take `@RequestBody`** — a JSON body is mandatory and query params are ignored. The wiki still documents only the old query-param/dot-notation form. We send the key BOTH ways so pre-7.15.3 installs keep working.
  - **Those four are also gated by `auth.allowApiStats`** (default `true`). With it off they reject a valid key while `t=caps` keeps working — so a failure there means "the user switched the stats API off", not "wrong key". It is indistinguishable from a wrong key at the HTTP level (both are the 500 above), which is why `isStatsApiGated()` is only consulted alongside a caps query already known to have succeeded.
  - **Timestamps are polymorphic.** `application.properties` sets `write-dates-as-timestamps=false`, yet upstream's own UI (`parseAppTimestamp` in `core/ui-src/js/indexer-statuses-controller.js`) accepts a raw number (epoch **SECONDS**, e.g. `1544551917.589`), a numeric string, and an ISO-8601 string for the same field — and a real 8.9.0 answers `/api/stats` with `"after": 1785283200.000000000`, i.e. decimal epoch seconds, so the property does not win. Send ISO-8601 for `after`/`before`. `parseHydraTimestamp` absorbs all three.
  - **`request.sortModel` is mandatory on the history endpoints.** `History.getHistory` guards its first sort branch with `if (sortModel != null)` and then dereferences `sortModel.getColumn()` unconditionally on the next line, while `HistoryRequest.sortModel` has no default — omit it and the call NPEs into a 500 (measured: with it, 200 + the `Page` envelope; without it, 500). `sortMode == 1` is ASC; **every other value, 0 included, is DESC**. `filterModel` does default, so it is safe to omit. `sortModel.column` is interpolated straight into native SQL, so only send whitelisted names.
  - **History paging is asymmetric**: the request `page` is 1-based (`PageRequest.of(page - 1, limit)`), the response `Page.number` is 0-based — so the next page is `number + 2`.
  - **`/api/stats` is expensive** — upstream aborts the calculation at 30 s, so give it a longer timeout and request only the flags you render. **Never omit `request`**: `ApiStatsRequest`'s no-arg constructor turns nearly every flag on. Naming trap: the request flag `avgIndexerUniquenessScore` populates the response field `indexerScores`.
  - **`/actuator/health/ping` is `permitAll`** (and `management.endpoints.web.exposure.include=*`), so it is a fine reachability ping but can never validate the key — the same trap as Autobrr's `/api/healthz/liveness`. Verified 200 on 8.9.0.
  - **`searching.wrapApiErrors`** (default `false`) makes an internal error return an *empty but valid* search result at 200, byte-identical to a genuine zero-result search. There is no way to tell them apart; the empty-state copy says so rather than the code guessing.
  - `indexers=` matches indexer **names**, not ids (`/api/stats/indexers` exposes no id at all). `o` defaults to **XML** — `o=json` is required. `limit` defaults to 100 and is capped at 100. Search result `link` is `<hydraUrl>/getnzb/api/<id>?apikey=<key>`, i.e. self-authenticating, but the download client has to be able to reach that host.
  - With auth configured AND `restrictSearch` + `restrictStats` + `restrictAdmin` all on, `HydraAnonymousAuthenticationFilter` grants no authorities and even `/api` demands HTTP Basic. Per-instance custom headers are the escape hatch.
- **Navidrome is three APIs behind one host, and only one of them is Subsonic.** All verified against `navidrome/navidrome@master` (v0.63.2):
  - **There is no API key.** `server/subsonic/middlewares.go:validateCredentials` handles exactly three shapes: `p` (plain, or `enc:` + hex), `t`+`s`, and `jwt`. `GetOpenSubsonicExtensions` advertises `transcodeOffset`, `formPost`, `songLyrics`, `indexBasedQueue`, `transcoding`, `playbackReport`, `topSongsByArtistId` — **not** `apiKey`. So `authShape` is `userPass`, but **not** `SERVICE_DEFAULTS.httpAuth` (nothing is HTTP Basic) — the qBittorrent/Deluge distinction. We send `t = md5(password + salt)`; upstream recomputes it from the `s` we send, so **any (salt, token) pair stays valid for the life of the password** and one salt per instance is enough. `lib/navidrome-normalize.ts` owns the hashing (md5 from `@noble/hashes/legacy.js`, the same dep `lib/http-auth.ts` uses for Digest).
  - **EVERY Subsonic error is HTTP 200.** `sendResponse` in `server/subsonic/api.go` sets no status for failures — the only exception is 429 for `ErrTooManyTranscodes`. A wrong password arrives as `200 {"subsonic-response":{"status":"failed","error":{"code":40,…}}}`. Same trap as NZBHydra2's `/api` mount: the probe and every call must read the BODY. `unwrapSubsonic` throws a `SubsonicApiError` carrying the numeric code; `isSubsonicAuthError` splits 40/41/42/43/44/50 (auth) from the rest (unreachable).
  - **`apiBasePath` is empty because there are three roots**: `/rest/*` (Subsonic v1.16.1), `/api/*` (the native react-admin API, `X-ND-Authorization: Bearer <jwt>` — `consts.UIAuthorizationHeader`), and `POST /auth/login` (`{username,password}` → `{token,id,name,username,isAdmin,subsonicSalt,subsonicToken}`). Paths self-prefix, the Cleanuparr / JellyStat pattern. Under a subpath deploy everything shifts together (`conf.Server.BasePath`).
  - **Total size and delete-missing exist ONLY on the native API.** `model/library.go`'s row carries `totalSongs/totalAlbums/totalArtists/totalFolders/totalFiles/totalMissingFiles/totalSize/totalDuration/lastScanAt/fullScanInProgress`, and one `GET /api/library` returns every library. Nothing in Subsonic reports size or the missing count. `DELETE /api/missing` **with no `id` param** routes to `Maintenance.DeleteAllMissingFiles` (`server/nativeapi/missing.go`); sending ids deletes only those.
  - **Admin gating splits the feature set.** `startScan` is behind subsonic's `adminOnly`; `/api/library` (and `/inspect`, `/config`, `/plugin`) behind nativeapi's `adminOnlyMiddleware`. `getScanStatus`, `getUser`, `getNowPlaying`, `search3`, `getAlbumList2`, `getPlaylists` are ungated. **`getUser?username=<u>` returns `adminRole`**, so admin-ness is discoverable without spending a login — which is what lets the Overview degrade instead of erroring.
  - **`POST /auth/login` is rate limited by default** (`conf.Server.AuthRequestLimit`, applied in `server/server.go:mountAuthenticationRoutes`; a warning is logged when it is disabled). Cache the JWT per instance and re-mint only on 401 — never per request. Session lifetime is `consts.DefaultSessionTimeout` = 48h, and `CheckClaims` also revokes on a `TokenEpoch` bump (password change). `server.JWTRefresher` returns a refreshed token in the `X-ND-Authorization` **response** header, which `serviceRequest` does not surface — hence the re-login-on-401 approach instead.
  - **A login token has no audience, so it works on Subsonic too.** `CreateToken` calls `userClaims(u, nil)`, and `CheckClaims` only enforces an audience when `len(c.Audience) > 0`. So `?jwt=<token>` is a valid Subsonic credential. We deliberately do NOT use it: `t`/`s` needs no login, is spec-standard, and keeps the whole read path working when `/auth/login` is rate-limited or proxied away.
  - **`getScanStatus` carries Navidrome extensions** beyond the spec's `scanning`/`count`: `folderCount`, `lastScan`, `error`, `scanType`, `elapsedTime`. **`startScan?fullScan=true|false`** blocks up to 3s waiting for the scanner to actually start (`pollTimeout` in `server/subsonic/library_scanning.go`) and then answers with the same `scanStatus` payload — give it a raised timeout. Note `/api/library`'s `fullScanInProgress` flips only for a FULL scan, so a running quick scan reads as idle there; fold `getScanStatus.scanning` in.
  - **`getNowPlaying` entries are `Child` + Navidrome extras**: `username`, `minutesAgo`, `playerId`, `playerName`, `state`, `positionMs`, `playbackRate`. `positionMs` is **milliseconds** while `duration` is **seconds** — mixing them reports 200x progress. There is no transcoding metadata and no client IP on this endpoint, so `transcoding` and `isLocal` are structurally absent, not unread.
  - **Timestamps that were never set come back as Go's zero time**, `0001-01-01T00:00:00Z`. Rendering that through `formatTimeAgo` claims the library was scanned two millennia ago; `lib/navidrome-normalize.ts` maps it to null.
  - **The native API is json-server dialect** (`deluan/rest`): paginate with `_start`/`_end`, sort with `_sort`/`_order`, and every list GET sets an **`X-Total-Count`** header. `serviceRequest` returns the parsed body only, so that header is unreachable today — read counts from `/api/library` instead of `/api/missing?_start=0&_end=0`.
  - **`/rest/getOpenSubsonicExtensions` is the only Subsonic route registered outside the auth group**, so it answers anonymously and is our `pingPath`. It therefore CANNOT validate credentials — the same trap as Autobrr's `/healthz/liveness` and NZBHydra2's `/actuator/health/ping`, which is why the connection probe uses `/rest/ping`.
  - The web UI is a react-admin **hash** router mounted at `/app` (`consts.URLPathUI`), so deep links are `<base>/app/#/<resource>/<id>/show`. `getCoverArt` takes Subsonic auth in the query string, so a plain `<Image>` can load it — strip the credential from the expo-image `cacheKey` (the `getPlexImageSource` idiom) or every cover re-downloads when the salt rotates.
- **Jellyfin gates every Emby-era credential shape behind one server flag, and it is off by default from 12.0.** Verified by reading `Jellyfin.Server.Implementations/Security/AuthorizationContext.cs` at the tags v10.8.13, v10.9.11, v10.10.7, v10.11.0, v10.11.11 and v12.0-rc7:
  - `EnableLegacyAuthorization` guards **`X-Emby-Token`, `X-MediaBrowser-Token`, `X-Emby-Authorization`, the `api_key` query param, and the `Emby` scheme name** on the Authorization header. The flag does not exist before **10.11.0**, defaults to `true` through the whole 10.11.x line (but is an admin-facing toggle there), and defaults to `false` on **12.0** (`MediaBrowser.Model/Configuration/ServerConfiguration.cs`). With it off a perfectly valid key produces a bare **401**, indistinguishable from a wrong key, which is exactly how it reached us as "Invalid Jellyfin token" (#399).
  - The two shapes read **unconditionally in every one of those versions** are `Authorization: MediaBrowser …` and the **`ApiKey`** query param (PascalCase, since `api_key` is the gated spelling). Those are what `lib/media-server-config.ts` sends: the header for API calls, `ApiKey` for `<Image>` URLs, which cannot carry headers.
  - **Send only `Token` in that header.** When the key is a *user* access token rather than a server API key, the token resolves to a device row and `GetAuthorizationInfoFromDictionary` writes any `Device`/`Version` parts you send **back onto that row**, renaming whichever client actually minted the token.
  - **Never clobber a caller-supplied `Authorization` header**: for Jellyfin that is a reverse proxy's Basic credential from Settings -> Network -> Custom Headers, and overwriting it locks the user out before Jellyfin sees the request. But leaving it there is not enough either, because `GetAuthorizationDictionary` reads `Authorization` FIRST and `GetAuthorization` returns **null** for any scheme that is not `MediaBrowser`: the proxy's `Basic` credential does not coexist with the token, it consumes the only header Jellyfin would have read, and the request goes out anonymous. So `applyMediaServerAuth` hands the key back as the `ApiKey` query param in that case, the only ungated shape left. Header first (keeps the key out of access logs), query param only as the fallback.
  - **Emby is not affected** and keeps `X-Emby-Token` + `api_key`: the flag is a Jellyfin-only addition, and Emby still documents both.
  - `/System/Info` is the connection probe rather than `/Users/Me`, which 400s for server-wide API keys because they carry no user context. API keys do satisfy its elevated policy, since `CustomAuthenticationHandler` grants `IsApiKey` requests the Administrator role.
- The `backend/dashboarr-backend/` Node.js service is in-tree and not a third-party API — its surface is whatever we define there.
- Where an instance hosts its own OpenAPI/Swagger (Radarr, Sonarr, Prowlarr, Bazarr, Jellyfin), prefer fetching the live spec from a real instance over the public docs when verifying field types or new endpoints — the live spec matches the running version exactly.
